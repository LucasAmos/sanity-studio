on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
env:
  SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3.0.2
        id: changes
        with:
          filters: |
            app:
              - 'apps/${{ inputs.name }}/**'
      - run: corepack enable
      - uses: actions/setup-node@v4
        if: ${{ steps.changes.outputs.app == 'true' }}
        with:
          node-version: 22
          cache: yarn
      - run: yarn install --immutable
        if: ${{ steps.changes.outputs.app == 'true' }}
      - name: ${{ inputs.name }}
        if: ${{ steps.changes.outputs.app == 'true' }}
        working-directory: apps/${{ inputs.name }}
        run: yarn deploy
